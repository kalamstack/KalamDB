{
  "name": "kalamdb-summarizer-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Small worker-only example using runAgent to enrich rows through a topic route",
  "scripts": {
    "start": "tsx src/agent.ts",
    "test": "node --import tsx --test tests/*.test.ts",
    "setup": "bash ./setup.sh"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "kalam-link": "file:../../link/sdks/typescript"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  }
}
